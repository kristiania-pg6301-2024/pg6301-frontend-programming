{
  "devDependencies": {
    "husky": "^9.1.6",
    "prettier": "^3.3.3",
    "typescript": "^5.6.2",
    "vitest": "^2.1.1"
  },
  "scripts": {
    "test": "prettier --check . && vitest --run && tsc --noEmit && npm run test:client",
    "test:dev": "vitest",
    "test:client": "cd client && npm test",
    "prepare": "husky"
  }
}
