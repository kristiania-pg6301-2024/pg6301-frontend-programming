{
  "devDependencies": {
    "husky": "^9.1.6",
    "prettier": "^3.3.3",
    "vitest": "^2.1.1"
  },
  "scripts": {
    "test": "vitest",
    "pre-commit": "vitest --watch=false && prettier --check .",
    "prepare": "husky"
  }
}
