{
  "devDependencies": {
    "husky": "^9.1.6",
    "prettier": "^3.3.3",
    "vitest": "^2.1.1"
  },
  "scripts": {
    "test": "prettier --check . && vitest --run",
    "test:dev": "vitest",
    "prepare": "husky"
  }
}
