{
  "devDependencies": {
    "husky": "^9.1.6",
    "prettier": "^3.3.3",
    "typescript": "^5.6.2"
  },
  "scripts": {
    "test": "tsc --noEmit && prettier --check .",
    "prepare": "husky",
    "dev": "cd client && npm run dev"
  }
}
