{
  "devDependencies": {
    "husky": "^9.1.6",
    "prettier": "^3.3.3"
  },
  "scripts": {
    "test": "prettier --check . && npm run test:client && npm run test:shared",
    "test:client": "cd client && npm test",
    "test:shared": "cd shared && npm test",
    "prepare": "husky"
  }
}
