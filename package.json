{
  "devDependencies": {
    "husky": "^9.1.6",
    "prettier": "^3.3.3",
    "typescript": "^5.6.2",
    "vitest": "^2.1.1"
  },
  "scripts": {
    "test": "prettier --check . && vitest --run && tsc --noEmit",
    "test:dev": "vitest",
    "prepare": "husky"
  }
}
